# Fitness-step-predictor
A machine learning web app built with Streamlit that predicts daily step count using activity data such as distance, calories burned, active time, and goal completion. It uses a trained Random Forest model with feature scaling to provide accurate and real-time predictions.
